# マルチテナント化 進捗

[multi-tenant-handoff.md](multi-tenant-handoff.md) §6.4 に従い、**issue の着手時と完了時に必ず更新する**。コンテキストが切れた別のエージェントが、このファイルだけを見て再開できる粒度で書く。

最終更新: 2026-09-24(キャッチアップ報告書を PR #74 で提出、H1 と H2 を待機中)

## 現在の状態

- フェーズ: **H1(キャッチアップ報告書のレビュー)待ち** と **#69 の H2 を並行**
- 統合ブランチ: `feature/topic-multitenant`(2026-09-23 に origin へ push 済み)
- 人間チェックポイント待ち:
  - H1: PR #74(`doc/multi-tenant-catchup.md`)のレビュー。特に 5.1 の 5 項目の判断
  - H2: GCP の無料トライアル開始・プロジェクト作成・gcloud ログイン(`doc/gcp-setup.md` §3)
- 作業体制(オーナー指示 2026-09-24): 親エージェントが具体的な指示を書き、安価なモデル(Sonnet)に下書き・実装させ、親が厳密にレビューする

## 作業ディレクトリ(git worktree)

| パス | ブランチ | 用途 |
|---|---|---|
| `~/Desktop/housework` | `mt/catchup-report` | キャッチアップ報告書(オーナーの handoff 修正コミット `ba331002` を含む) |
| `~/Desktop/housework-wt/integration` | `feature/topic-multitenant` | 進捗ファイルの更新、統合 |
| `~/Desktop/housework-wt/mt-69` | `mt/69-gcp-foundation` | #69(`doc/gcp-setup.md`、`.gitignore`、今後 `infra/terraform/`) |

## 着手中

| issue | 担当(エージェント名など) | ブランチ | 状況・次の一手 |
|---|---|---|---|
| §5 キャッチアップ | 親 + Sonnet(章ごとの下書き → 事実確認 → 親のレビュー) | `mt/catchup-report`(PR #74) | 報告書を提出済み。H1 の承認待ち。承認されたら PR #74 のマージは人間、次は #36 と #35 |
| #69 G0 | 親(人間と伴走) | `mt/69-gcp-foundation`(push 済み、PR 未作成) | 手順書と `.gitignore` をコミット済み。gcloud 586.0.0 / Terraform 1.16.4 をインストール済み。人間の H2 手順 1〜4 待ち → 次は `doc/gcp-setup.md` §4(state バケット、`infra/terraform/`) |

## 完了(統合ブランチにマージ済み)

| issue | PR | マージ日 | 備考 |
|---|---|---|---|
| — | — | — | — |

## ブロック中・未解決の疑問

| 内容 | 誰の判断が必要か | 状態 |
|---|---|---|
| 無料トライアルは 90 日で終わり、アップグレードしないと Pub/Sub が停止・削除される(#69 #73 にコメント済み)。終了前に「アップグレード」か「outbox を旧方式に戻す」かを決める | オーナー | トライアル終了予定日の 10 日前までに判断 |
| 予算アラートの通貨は請求先アカウントの通貨に合わせる必要がある(#69 本文は USD 指定) | H2 で通貨を確認すれば解消 | H2 待ち |

## handoff の補正(キャッチアップで判明。詳細は `doc/multi-tenant-catchup.md` 5.4)

- **本番の PostgreSQL は 17.7**(handoff §3 は 16 前提)。#41 の Testcontainers と #67 のリハーサルは `postgres:17` を使う。
- **ローカル DB(`housework-db`)には shopping の V22〜V25 が適用済み**で、統合ブランチの V20〜V22 と衝突する。H1 で承認されれば、マルチテナント作業は専用コンテナ(`postgres:17`、ポート 5433)で行い、#36 で Gradle の Flyway / jOOQ の接続先を上書き可能にする。既存の `housework-db` には触らない。
- **Flyway は起動時ではなく、最初に `DatabaseConfig.dataSource` に触れたときに走る**(lazy 初期化)。#37 で 2 本目のプールを作るときは、先にオーナー側の migration を済ませること。
- 本番 DB の接続ユーザーは superuser(V21 の CREATE ROLE は通る)、`max_connections` は 100。

## 順序の決定(親エージェント)

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-09-24 | **#44 → #51** の順にマージする | #51 が先だと、register が tenantId 無しで `CreateMemberUseCase` を呼べなくなる。409 の共通例外も #44 で作る |

## 契約の変更履歴(#34 を変更したら記録)

| 日付 | 変更 | 承認 |
|---|---|---|
| — | — | — |

## 運用上の教訓

- 2026-09-23: 16 並列の読解ワークフローが利用上限に達し、22 体すべてが成果物を返さずに終わった(約 102 万トークン消費)。原因は、成果を最後にまとめて返す設計、同時実行上限(このマシンは 8)、失敗時の自動再試行、issue 担当にもコード裏取りをさせた重複読み。以後は、エージェントに節ごとのファイル保存をさせ、読むファイルを明示的に限定し、並列数を抑える。

## 次にやること(優先順)

1. H1(PR #74)の承認を待つ。承認まで実装に入らない
2. H2 完了後、#69 の `infra/terraform/`(state バケット、API 有効化、予算アラート)→ plan を人間に見せてから apply
3. H1 承認後: #36(V22 backfill の新規作成、旧 V22 → V23 の rename、V21 冒頭コメント修正、jOOQ 再生成)と #35
4. #35 と並行で #62 #63 #64(フロント)、#71(Pub/Sub クライアント基盤)
